# Whole Brain Perturb-seq — Python dependencies
# Used by scripts/, analysis_lib/, and Jupyter notebooks (Figure 1, 2, 3, DEGs).

# Core single-cell / AnnData
scanpy>=1.9.0
anndata>=0.9.0

# Numeric / data
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Plotting
matplotlib>=3.5.0
seaborn>=0.11.0

# Utilities
tqdm>=4.62.0
joblib>=1.1.0
pyyaml>=5.4.0
openpyxl>=3.0.0

# Perturbation analysis (Figure 3, etc.)
pertpy>=0.6.0

# Data download (scripts/download.py)
huggingface_hub>=0.16.0

# Optional for Figure 1 GPU pipeline (not required for standard runs):
# dask[array] dask-cuda cupy-cuda12x rmm rapids-singlecell zarr hnswlib psutil
